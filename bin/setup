#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -e

if ! command -v cargo >/dev/null; then
  echo "ERROR: Rust is not installed. Please install Rust and try again (https:///rustup.rs)."
  exit 1
fi

bundle install --jobs=3 --retry=3

bundle exec rake compile
